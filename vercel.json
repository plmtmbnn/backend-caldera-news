{
    "version": 2,
    "builds": [
        {
            "src": "./server.ts",
            "use": "ts-node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "./server.ts"
        }
    ]
}